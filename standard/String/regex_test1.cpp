#include <bits/stdc++.h>

using namespace std;

int main()
{
    string s = "We cannot afford the price of the house.";
    regex self_regex("CANNOT AFFORD", regex_constants::ECMAScript | regex_constants::icase);
    return 0;
}
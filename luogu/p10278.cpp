#include <bits/stdc++.h>

using namespace std;

constexpr int MAX_N = 1e5 + 10, MAX_P = 2e5 + 10;
typedef pair<int, int> P;
typedef long long ll;

P bar[MAX_P];

int main()
{
    int n, p;
    cin >> n >> p;
    return 0;
}
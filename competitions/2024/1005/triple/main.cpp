#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    if (n >= 3) {
        cout << 1 << ' ' << 2 << ' ' << 3 << endl;
    } else {
        cout << -1 << ' ' << -1 << ' ' << -1 << endl;
    }
    return 0;
}
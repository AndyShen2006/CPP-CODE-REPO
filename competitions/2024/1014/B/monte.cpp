#include <bits/stdc++.h>
#include <ext_random.hpp>

using namespace std;

int main()
{
    int n, m;
    int cnt;
    double sum;
    int cycle = 10000;
    cin >> n >> m;
    for (int i = 1; i <= cycle; i++) {
        cnt = 0;
        sum = 0;
        while (sum < m) {
            int r = randint(1, m);
            if (r == 0) {
            }
        }
    }
    return 0;
}